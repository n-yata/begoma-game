<!-- sdd:begin -->
<!--
  この区間は kit が生成する。**手で編集しない。** 更新時に上書きされる。
  プロジェクト固有の事情は、下の「プロジェクト固有の設定」（マーカーの外）に書くこと。
  そこは更新時も保持される。
-->

# begoma-game

スペック駆動開発の進め方を定義する。**作業を始める前に本ファイルを読むこと。**

## 必ず守ること

以下は**索引を辿らなくても分かる位置**に置いてある。これだけは読み飛ばさない。

### 1. コミット前にセキュリティレビューを実施する

**コミットの前に、必ず `security-engineer` にレビューを依頼する。**

- 対象は変更されたすべてのファイル
- 依頼時は**ハードコーディング検出**の観点（URL / エンドポイント・シークレット・
  クラウドアカウント情報・ドキュメント内の機密）を必ず含める
- **Critical / High の指摘があれば、修正してからコミットする**
- レビュー結果（Critical / High の有無と対応）を `retrospective.md` に記載する。
  マルチリポジトリ構成では、これが実施の**唯一の証跡**になる

### 2. ブランチ運用

- **実装・修正作業は feature ブランチを作成して開始する。`main` への直接コミットは避ける**
- 作業完了時の順序: ①振り返り作成 → ②セキュリティレビュー → ③コミット → ④PR → ⑤ブランチ削除
- `--no-verify` 等によるフックのスキップは**禁止**
- **ステージングは対象を名指しする**（`git add -A` / `git add .` を使わない）
- 自分の作業範囲外の未コミット変更を見つけたら、**コミットに巻き込まずユーザーに確認する**

### 3. 同じ事実を2箇所に書かない

正本は必ず1つ。もう一方は参照リンクにする。二重管理は、更新が片方にしか入らず
**残りが静かに陳腐化する**。

### 4. 作業ごとにステアリングを切る

作業ごとに `.steering/[YYYYMMDD]-[タスク名]/` を作成する（`20260614-add-user-profile` 形式）。

| ファイル           | 内容                                             |
| ------------------ | ------------------------------------------------ |
| `requirements.md`  | 今回の要求内容                                   |
| `design.md`        | 実装アプローチ                                   |
| `tasklist.md`      | 具体的なタスクリスト                             |
| `retrospective.md` | 実装後の振り返り（`tasklist.md` とは別ファイル） |

**軽微な作業でも、残すべき学び・申し送りがあれば `retrospective.md` だけ**を残す。
学びが何もない軽微作業（typo 修正など）は作成不要。

**作業計画・実装・検証時は `/flow-steering` を使う**（モード1=計画 / 2=実装 / 3=振り返り）。

## 規約の正本

**汎用規約の正本は kit リポジトリの `reference/rules/*.md`。このプロジェクトには複製しない。**
索引は `/kit-guidelines` で引ける。

以下、`<kit>` は kit リポジトリのパスを指す。**実体は下の「kit の場所」で定義する。**

| 知りたいこと                                       | 参照先                                    |
| -------------------------------------------------- | ----------------------------------------- |
| コーディング規約・Git 運用・テスト戦略・命名規則   | `<kit>/reference/rules/`（索引は `/kit-guidelines`）    |
| シークレット管理・ロギング・依存管理・図版         | `<kit>/reference/rules/`                            |
| 使用しているエージェント固有の設定・拠点外起動抑止 | `<kit>/reference/host-rules.md`                |
| 環境変数一覧・セットアップ手順・CI/CD              | `docs/baseline/development-guidelines.md` |
| このプロジェクトのディレクトリ構造                 | `docs/baseline/repository-structure.md`   |
| 依存方向・レイヤー責務・技術選定                   | `docs/baseline/architecture.md`           |

> **規約は参照するだけで、このリポジトリには存在しない。** kit を書き換えれば
> 参照している全プロジェクトに即座に効く。規約を変えたい場合は kit へ還元する
> （`/kit-contribute`）。プロジェクト固有の差分は
> `docs/baseline/development-guidelines.md` に理由付きで書く。

> **`.claude/skills/`・`.claude/agents/`・`.claude/hooks/` だけはコピー。**
> ホストが実体を走査する仕様のため参照にできない。**編集しても次の更新で失われる。**

> ⚠️ **kit が手元に無いと規約が読めない。** クローン直後・CI・kit を持たないメンバーでは
> 参照が解決できない。**規約が読めない状態で作業を進めないこと**（読めたことにして
> 推測で進めるのが最悪の失敗）。読めなければ kit を用意するか、その旨を報告して止まる。

## スペック駆動開発の基本フロー

1. **ドキュメント作成**: 永続ドキュメント（`docs/`）で「何を作るか」を定義
2. **作業計画**: ステアリングファイル（`.steering/`）で「今回何をするか」を計画
3. **実装**: `tasklist.md` に従って実装し、進捗を随時更新
4. **検証**: テストと動作確認
5. **更新**: 必要に応じてドキュメント更新

### ドキュメント作成時

**1ファイルずつ作成し、必ずユーザーの承認を得てから次に進む。**

```
「[ドキュメント名]の作成が完了しました。内容を確認してください。
承認いただけたら次のドキュメントに進みます。」
```

### 実装前の確認

1. 本ファイルを読む
2. 関連する永続ドキュメント（`docs/`）を読む
3. 既存の類似実装を検索する
4. 既存パターンを理解してから実装開始

## ディレクトリ構造

`docs/` は **`baseline/`（指標）＋ `specs/`（成果物）** の2本立て。直下には実ファイルを置かない。

```
docs/
  baseline/                    ← 常に参照する指標。6ドキュメント＋ideas/
    ideas/                       前段（壁打ち下書き・技術調査メモ。自由形式）
    product-requirements.md      要件定義（PRD）— 何を/なぜ作るか
    functional-design.md         基本設計（機能設計書）— どう実現するかの正本
    architecture.md              基本設計（システム構造・技術選定）
    repository-structure.md      詳細設計（具体的なディレクトリ構造）
    development-guidelines.md    横断（このプロジェクト固有の規約差分）
    glossary.md                  横断（ユビキタス言語）
  specs/                       ← 各フェーズで参照・作成する成果物（実装詳細）
    1_requirements/  2_basic-design/  3_detail-design/
    4_unit-test/  5_integration-test/  _shared/
```

### `baseline/` と `specs/` の役割分担

**混同しないこと。**

|                | `docs/baseline/`                               | `docs/specs/`                                                  |
| -------------- | ---------------------------------------------- | -------------------------------------------------------------- |
| 読むタイミング | **常時参照する**（必ず見る北極星）             | **オンデマンド参照**（必要なときだけ）                         |
| 読者・目的     | 内部の意思決定指標。ぶれてはいけない           | **顧客へ提出するための成果物**。工程ごとに体裁・網羅性を整える |
| 性質           | `baseline/` 単体で意思決定が完結する状態を保つ | `baseline/` を展開・具体化し、提出品質に仕上げる               |

運用規律:

1. **依存は一方向**: `specs/` は `baseline/` を正本として参照する
2. **決定は `baseline/` に昇格させる**: 仕様の「決定」を `specs/` で確定して終わりにしない
3. **同じ事実は1箇所**: `specs/` 側は「`baseline/` のどこが正本か」のポインタだけ持つ

### baseline の6ドキュメントと工程の対応

| ファイル                    | 工程     | 内容                                                                                              |
| --------------------------- | -------- | ------------------------------------------------------------------------------------------------- |
| `product-requirements.md`   | 要件定義 | PRD。何を/なぜ作るか                                                                              |
| `functional-design.md`      | 基本設計 | データモデル・中核アルゴリズム・API/画面/ユースケースの一覧など、**ぶれてはいけない重要点の正本** |
| `architecture.md`           | 基本設計 | システム構造・技術選定                                                                            |
| `repository-structure.md`   | 詳細設計 | 技術スタックを反映した具体的なディレクトリ構造                                                    |
| `development-guidelines.md` | 横断     | **このプロジェクト固有の規約差分**（汎用規約は `<kit>/reference/rules/`）                                   |
| `glossary.md`               | 横断     | ユビキタス言語                                                                                    |

> **重要**: `baseline/` がカバーするのは **要件定義 ＋ 基本設計 ＋ 横断規約** まで。
> データモデルは概念モデルまでが `baseline/` の担当で、論理設計と物理スキーマは
> `docs/specs/3_detail-design/db/` が担当する。テストケース仕様は
> `docs/specs/4_unit-test/`・`docs/specs/5_integration-test/` が担当する。
> それ以外の詳細設計の残り・実装・テストの実施は `.steering/[日付]-[作業名]/` が担う。

### 配置の判断基準

1. **まず baseline か specs か** — 常時参照する指標か、必要なときだけ見る実装詳細か
2. **baseline 内での工程分類** — 「何を/なぜ」「どう実現するか」「どこに何が居るか」「工程非依存の規約・用語」
3. **基本設計と詳細設計の境界** — 論理・外部から見た設計＝基本設計、物理・具体＝詳細設計
4. **設計高度を保つ** — baseline は設計まで。具体的な関数分割・実コードは `.steering/` と実装リポジトリへ
5. **迷ったら重要点側に倒す** — 「ぶれたら困るか」を基準に、迷った内容は baseline に残す

> **クイック判定**: 「必要な時にだけ参照する実装詳細」→ `docs/specs/` /
> 「実ファイル名・ツリーが出てくる」→詳細設計 / 「工程に依らない規約・用語」→横断 /
> 「論理的な機能の実現方法・ぶれてはいけない重要点」→基本設計 / 「何を・なぜ」→要件定義。

## 開発プロセス

### 初回セットアップ

1. `/flow-grill-with-docs` でアイデアを壁打ちし `docs/baseline/ideas/` に書き出す
2. `/flow-setup-project` で baseline 6ドキュメントを対話的に作成
3. `/flow-add-feature [機能]` で機能実装

### 日常的な使い方

**基本は普通に会話で依頼してよい。**

```
> PRDに新機能を追加してください
> /flow-add-feature ユーザープロフィール編集
> /flow-review-docs docs/baseline/product-requirements.md
```

<!-- sdd:end -->

## kit の場所

**`<kit>` の実体はここで定義する。正本はこの1行だけ。**

```
<kit> = ../spec-kit
```

このパスは `.claude/settings.json` の**2箇所**にも入っている必要がある。

| 設定                                | 無いとどうなるか                                   |
| ----------------------------------- | -------------------------------------------------- |
| `permissions.additionalDirectories` | kit を**読めない**。規約に到達できない             |
| `permissions.deny`                  | kit を**書き換えられる**。全プロジェクトに即座に効く |

> 🚨 **kit を移動したら3箇所すべてを直す。** 片方だけ直すと
> 「設定上は参照できることになっているのに読めない」、あるいは
> **「読めるが拒否だけ効いていない」**という状態になる。後者は症状が出ない。
>
> `deny` には相対パスと絶対パスの両方が入っている。相対（`../` を含む形）が
> 解釈されない環境でも絶対パス側で効かせるための二枚構えなので、**両方直すこと**。

> **ここはマーカーの外側なので、更新時も保持される。** kit を移動した場合は
> この行と `settings.json` の両方を直すこと。

## プロジェクト固有の設定

> **ここから下がこのプロジェクトの領域。** 上のマーカー区間は kit の配布物であり
> 更新時に上書きされるが、**ここは保持される**。
>
> 汎用規約から意図的に外れる点は、理由とともに
> `docs/baseline/development-guidelines.md` に書く。

<!-- 例:
- 技術スタック上の制約
- 特殊な運用ルール（マルチリポジトリ構成での作業拠点など）
- チーム固有の取り決め
-->

（未記入）
