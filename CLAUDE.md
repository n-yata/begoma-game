# begoma-game

**このプロジェクトの規約・進め方の正本は [`AGENTS.md`](./AGENTS.md)。**
作業を始める前に必ずそちらを読むこと。

本ファイルは、`AGENTS.md` を自動で読まないツールのために置いてあるポインタであり、
**規則の本文を持たない**。ここに規約を書き足すと `AGENTS.md` と二重管理になり、
更新が片方にしか入らず静かに食い違う。

| 知りたいこと                       | 参照先                                    |
| ---------------------------------- | ----------------------------------------- |
| 進め方・必ず守ること               | `AGENTS.md`                               |
| 汎用規約（コーディング・Git 運用） | `<kit>/reference/rules/`（索引は `/kit-guidelines`）    |
| 使用しているエージェント固有の設定 | `<kit>/reference/host-rules.md`                |
| このプロジェクト固有の規約差分     | `docs/baseline/development-guidelines.md` |

> `<kit>` は kit リポジトリのパス。**実体は `AGENTS.md` の「kit の場所」で定義する。**
> 規約はこのリポジトリに複製されていない。
